info:
  name: Get directory
  type: http
  seq: 1

http:
  method: GET
  url: "{{SITE_ROOT}}/api/dir/{{path}}"
  auth: inherit

runtime:
  variables:
    - name: path
      value: "{{?directory path}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
