info:
  name: Delete file
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{SITE_ROOT}}/api/file/{{path}}"
  auth: inherit

runtime:
  variables:
    - name: path
      value: "{{?file path}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
