info:
  name: Get notes
  type: http
  seq: 1

http:
  method: GET
  url: "{{SITE_ROOT}}/api/note/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
