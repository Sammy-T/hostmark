info:
  name: Get note
  type: http
  seq: 3

http:
  method: GET
  url: "{{SITE_ROOT}}/api/note/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "{{?id}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
