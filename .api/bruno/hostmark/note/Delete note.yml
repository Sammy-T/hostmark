info:
  name: Delete note
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{SITE_ROOT}}/api/note/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "{{?id}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
