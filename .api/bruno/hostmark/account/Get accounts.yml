info:
  name: Get accounts
  type: http
  seq: 3

http:
  method: GET
  url: "{{SITE_ROOT}}/api/account/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
