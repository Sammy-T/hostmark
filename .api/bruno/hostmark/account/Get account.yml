info:
  name: Get account
  type: http
  seq: 2

http:
  method: GET
  url: "{{SITE_ROOT}}/api/account/{{username}}"
  auth: inherit

runtime:
  variables:
    - name: username
      value: "{{?username}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
